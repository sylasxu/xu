# H5 v5.2 最小验收清单

> 更新日期：2026-03-02  
> 适用范围：`apps/web` 当前阶段（仅 `/chat` + `/invite/[id]`）

## 1) 范围约束验收

| 检查项 | 验收标准 | 状态 | 证据 |
|---|---|---|---|
| H5 路由收敛 | 仅保留 `/chat`、`/invite/[id]`（含 `/` 重定向） | ✅ | `apps/web/app` 下仅 `chat`、`invite`、`page.tsx` |
| 消息中心移除 | H5 不提供 `/message` 页面 | ✅ | `apps/web/app/message/page.tsx` 已删除 |
| 无登录依赖 | Chat 请求不携带凭证，欢迎接口不依赖登录态 | ✅ | `apps/web/app/chat/page.tsx` 中 `credentials: "omit"` |

## 2) `/chat` 游客对话页验收

| 检查项 | 验收标准 | 状态 | 证据 |
|---|---|---|---|
| 页面结构 | 移动端单栏容器，最大宽度受控 | ✅ | `max-w-[430px]`（`apps/web/app/chat/page.tsx`） |
| AI 流式对话 | 通过 `POST /ai/chat` 流式返回 | ✅ | `DefaultChatTransport({ api: ${API_BASE}/ai/chat })` |
| 欢迎区 + 芯片 | 无消息时展示欢迎建议；底部显示去重 chips | ✅ | `WelcomeScreen` + `promptChips` 去重逻辑 |
| 输入框交互 | 空输入单行；有输入转双行（tools + send） | ✅ | `hasInput ? ... : ...` 分支布局 |
| 新对话行为 | 顶部菜单支持重置会话 | ✅ | `resetChat()` 清空 `messages` 与 `input` |

## 3) `/invite/[id]` 邀请函页验收

| 检查项 | 验收标准 | 状态 | 证据 |
|---|---|---|---|
| SSR 数据获取 | 服务端拉取活动公开数据 | ✅ | `getPublicActivity()` 使用服务端 `fetch` |
| OG 元数据 | `openGraph` + `twitter` 包含默认图 | ✅ | `apps/web/app/invite/[id]/page.tsx` |
| 默认 OG 图 | 提供可访问静态资源 | ✅ | `apps/web/public/og/invite-default.svg` |
| 微信内分支 | 已配置 `NEXT_PUBLIC_WECHAT_APPID` 时显示直跳按钮 | ✅ | `getMiniProgramUrl()` + `WechatRedirect` |
| 未配置兜底 | 未配置 appid 时提示配置，不展示假链接 | ✅ | `miniProgramUrl ? ... : 配置提示` |
| 非微信分支 | 展示二维码 + invite 链接 + 小程序路径 | ✅ | `getMiniProgramQrImageUrl()` 与二维码 `<img>` |

## 4) 构建与类型验收

| 检查项 | 验收标准 | 状态 | 结果 |
|---|---|---|---|
| 生产构建 | `bun run build` 通过 | ✅ | 2026-03-02 本地执行通过 |
| 路由产物 | 输出包含 `/chat`、`/invite/[id]` | ✅ | Next build 路由表校验通过 |

## 5) 上线前剩余人工检查（真机）

| 检查项 | 验收标准 | 状态 |
|---|---|---|
| 微信内邀请函跳转 | iOS/Android 微信内点击可拉起小程序 | ⏳ 待真机验证 |
| 非微信扫码链路 | 浏览器打开邀请函可扫码进小程序详情页 | ⏳ 待真机验证 |
| Chat 长文本输入 | 多行输入滚动与发送按钮位置稳定 | ⏳ 待真机验证 |
